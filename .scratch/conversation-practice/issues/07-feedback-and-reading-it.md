# 07 — Feedback, and reading it after an Attempt

**What to build:** When judging finishes, the Trainee reads coaching prose about the Attempt
they just took — told what to do differently, not merely what they got wrong, and pointed at
specific moments in their own Attempt rather than at conversations in general. Written *to*
them, not about them: coaching, not a performance review.

Feedback is a second, separate call. It receives the Assessment *and* the Transcript, and is
instructed that the verdicts are fixed — its job is to explain and advise, not to re-open
them.

The separation is the point. A single call producing verdicts *and* prose will write "you
built great rapport!" directly above a scorecard showing four failures — the exact case of a
Trainee who is warm, courteous and professional but never asks what happened and scores 2 of
6. Leadership then believes one of the two and we cannot control which. Grade first, coach
second.

This replaces the Feedback stub from ticket 05. The persisted Attempt now holds all three:
Transcript, Assessment, and Feedback — which is what makes a tuning run that went wrong
readable afterwards instead of guessed at.

Getting the tone right is ticket 12's job. This ticket has to produce the Feedback, persist
it, and put it in front of the Trainee.

**Measured 2026-07-29:** an Assessment takes 6.5–8.5 seconds, and that figure does not grow
with the length of the Attempt — 3, 9 and 22-turn Transcripts all landed in the same band,
so the cost is the grader's reasoning rather than the Transcript it reads. Feedback is a
second sequential call and writes prose rather than short quotes, so budget roughly twenty
seconds of judging in total. That is short enough for the page to block on the completion
request and show ticket 05's judging-in-progress state throughout; polling is not worth
building. Re-measure if the Assessment prompt grows much:
`npx tsx .scratch/conversation-practice/check-assessment.ts --live`.

Note the completion endpoint currently answers `204` and `CompleteAttempt` returns
`Promise<void>`, discarding the numbered `Attempt` that `storeAttempt` already returns —
nothing can carry Feedback to the page until that changes. A read path for the latest
Attempt is worth building here rather than in ticket 11: if the blocking request ever fails
or times out the Attempt is already safely on disk, and without a read path that looks like
data loss when it is not.

**Blocked by:** 06 — Assessment against the Rubric.

**Status:** ready-for-agent

- [ ] Feedback is generated by a separate call from the Assessment, receiving both the
      Assessment and the Transcript.
- [ ] The Feedback call is instructed not to re-judge; the verdicts it receives are fixed.
- [ ] Assessment demonstrably runs before Feedback.
- [ ] The Feedback is prose addressed to the Trainee, referring to specific moments in their
      Attempt.
- [ ] The persisted Attempt holds the Transcript, the Assessment, and the Feedback.
- [ ] The Trainee reads the Feedback once judging completes, without reloading or navigating
      anywhere manually.
